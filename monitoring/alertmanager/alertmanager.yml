route:
  receiver: blackhole
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  routes:
  - matchers: ['severity="critical"']
    receiver: critical-operator-placeholder
receivers:
- name: blackhole
- name: critical-operator-placeholder
