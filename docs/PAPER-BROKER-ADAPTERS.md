# Paper Broker Adapters

`PaperExecutionProvider` requires a `paper-` provider ID, accepts only deterministic fixture prices, exposes the canonical adapter methods and contains no HTTP, socket, credential or production endpoint implementation. No external paper account was used or certified.
