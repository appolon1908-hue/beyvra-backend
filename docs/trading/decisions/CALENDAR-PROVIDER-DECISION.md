# Economic-calendar provider decision package

The calendar schema and staging fixture boundary are ready. No live provider,
forecast, or actual-value source is approved in the current environment.

Classification: `BOUNDARY_ONLY_CREDENTIALS_REQUIRED`.
Missing values must remain null; fixtures are never presented as live data.
