# Provider approval gate

Provider activation requires a staging approval reference, credential
reference, license/redistribution decision, supported-symbol list, delay
classification, owner, and expiry. A credential existing on disk is not
approval. Missing approval or credentials leaves the adapter disabled.
