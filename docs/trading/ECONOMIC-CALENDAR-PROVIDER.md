# Economic calendar provider

Calendar values are provider facts with source, timestamp, revision, and
license metadata. Forecast and actual fields remain null when the provider does
not supply them; the system never fabricates values. A fixture boundary may be
used for staging UI tests but is labeled synthetic.
