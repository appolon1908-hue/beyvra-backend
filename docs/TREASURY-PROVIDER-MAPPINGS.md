# Treasury Provider Mappings

Broker, custody, and clearing identifiers map logical treasury locations to fixture/sandbox references. Mappings do not confer balance authority and are hidden from customer output. Live provider liquidity reads are absent.
