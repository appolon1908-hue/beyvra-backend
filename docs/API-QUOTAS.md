# API quotas

Plan entitlements carry bounded limit values and units. Persistent policy is authoritative; future Redis counters must fail closed or use an explicitly approved degraded policy. Account identifiers must not be metric labels.
