# AML authority

AML is independent from KYC: `NOT_SCREENED`, `PENDING`, `CLEARED`, `REVIEW_REQUIRED`, `BLOCKED`. Only `CLEARED` can permit eligibility. Missing, stale, review, and blocked states fail closed. AML evidence and refresh dates must be audited without raw PII.
