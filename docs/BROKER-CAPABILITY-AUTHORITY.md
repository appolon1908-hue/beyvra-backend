# Broker Capability Authority

Provider identity, environment, governance, health and verified capability records are persisted separately. Capabilities require an explicit source (`OFFICIAL_PROVIDER_DOC`, `PAPER_ACCOUNT_PROBE`, `MANUAL_APPROVAL`, or `FIX_SESSION_PROFILE`), version and verification time. Live capabilities are neither seeded nor exposed. Current providers are deterministic simulation and paper fixtures only.
