# Routing Decision Evidence

Each decision retains all candidates and rejection codes, selected provider/venue, score, mode, policy version, reference price, market/pricing/risk snapshot hashes, request hash and deterministic evidence hash. Decisions and candidates cannot be edited or deleted. Corrections are additive records with an incremented `revision` and protected `supersedes` link.
