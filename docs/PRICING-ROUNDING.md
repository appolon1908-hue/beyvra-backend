# Pricing rounding

Amounts and rates use `Decimal`. Currency-specific, effective-dated rounding policies define precision and approved mode. The initial engine uses explicit half-up rounding and never language-default binary floating point.
