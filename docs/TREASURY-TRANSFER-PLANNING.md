# Treasury Transfer Planning

The deterministic simulation planner prioritizes: segregation, hard obligations, buffer preservation, fewer moves, simulated cost, timing, then concentration. Sources must be active, tenant-owned, and non-client-segregated. Plans are idempotent and can become `SIMULATED`; no execution state or live endpoint exists.
