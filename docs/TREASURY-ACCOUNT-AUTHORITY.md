# Treasury Account Authority

`TreasuryAccount` identifies logical simulated locations by tenant, institution, optional subaccount, environment, account type, and segregation class. Allowed environments are `SIMULATION`, `PAPER`, and `SANDBOX_REFERENCE`; `LIVE` is intentionally absent. External references are excluded from customer serialization.
