# Pricing RBAC

Customer APIs expose only safe plans, resolved entitlements, and previews. Future operator mutation APIs require dedicated pricing/entitlement roles, tenant scoping, append-only audit, and maker/checker with no self-approval. Support roles cannot modify policy.
