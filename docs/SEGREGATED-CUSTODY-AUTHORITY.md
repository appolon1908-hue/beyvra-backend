# Segregated custody authority

Segregated mappings preserve a one-to-one subaccount relationship. A partial
unique PostgreSQL constraint prevents one active provider reference from being
assigned incompatibly. Provider references are masked in operator responses
and absent from customer contracts.
