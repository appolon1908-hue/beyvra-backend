# Broker account mapping

Canonical institution/subaccount identifiers remain authoritative. External
references are mappings only, protected by collision constraints and masked in
operator output. They are never returned by customer APIs.
