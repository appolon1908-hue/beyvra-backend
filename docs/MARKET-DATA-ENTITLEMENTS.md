# Market-data entitlements

Realtime requires both licensed provider capability and `MARKET_DATA_REALTIME`. Otherwise a licensed delayed feed requires `MARKET_DATA_DELAYED`; absent both, access is `NOT_AVAILABLE`. A plan alone never establishes provider entitlement.
