# NATS JetStream checkpoint

Not deployed in staging. Streams, retention, deduplication, consumers and
dead-letter policy require an approved private deployment and load test.
JetStream must never become the order or wallet source of truth.
