# Configuration authority

Definitions store metadata and safe defaults only. Snapshots bind normalized non-secret hashes to a release and environment. Drift compares declared and runtime hashes. Operator responses never contain values, defaults, secret references, or credentials.
