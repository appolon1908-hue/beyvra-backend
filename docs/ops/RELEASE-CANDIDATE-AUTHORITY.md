# Release candidate authority

The immutable manifest binds SHAs, image digests, migration/OpenAPI/SBOM/config/flag hashes, and test/security evidence. Mutable image references and incomplete hashes are rejected. Any candidate change requires a new manifest and evidence root.
