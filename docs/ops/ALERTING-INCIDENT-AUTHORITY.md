# Alerting and incident authority

Incidents use SEV0–SEV4 and controlled lifecycle transitions. A deduplication key prevents dependency-driven storms. Acknowledge and resolve endpoints are role-bound, reason-coded, and audited. Alerts cover availability, latency, stale data, outbox/consumer lag, DB/Redis saturation, reconciliation, kill switches, flags, backup, and restore.
