# Degraded mode authority

Health is evidence; operational mode is permission. Precedence is global/security/financial/execution halt, dependency failure, capacity restriction, freshness, normal. Missing high-risk halt state resolves to `HALTED`. Capabilities are explicit for `HEALTHY`, `DEGRADED`, `READ_ONLY`, `SIMULATION_ONLY`, `HALTED`, and `UNAVAILABLE`.
