# Capacity authority

Every claim binds service, dimension, environment, exact SHA, test time, and evidence. The default safe operating limit is 70% of a tested limit and cannot exceed it. Missing evidence is rejected. No production capacity is claimed by this local certification.
