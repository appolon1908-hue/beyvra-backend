# Beneficial-owner reference authority

Only opaque references issued by a named external compliance or identity
authority are stored. Email, names, billing, and activity are never used to
infer ownership. Customer APIs omit these mappings; operator output masks the
opaque reference. No identity documents are stored.
