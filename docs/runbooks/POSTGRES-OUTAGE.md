# PostgreSQL outage

Fail all state changes closed; never report success. Preserve outbox authority, restore PostgreSQL through infrastructure ownership, verify migrations, health, and full-stack reconciliation before traffic.
