# Settlement Instruction Missing

Verify trade validation, allocation, obligations, policy calendar, transaction rollback, and outbox. Retry only through the idempotent service after root cause is known. Confirm one instruction and a clean reconciliation before recovery closure.
