# Financial reconciliation failure

1. Treat `activation_ready=false` as a hard financial-readiness block.
2. Preserve the report digest, candidate SHA, environment, check count, and
   bounded incident candidate. Do not paste raw wallet/provider/PII data into
   alerts or tickets.
3. Determine which of the ten violation classes fired and compare the original
   read-only application and Financial Service evidence under authorized
   access.
4. Do not post a ledger correction, release a reservation, retry a settlement,
   or alter projection state automatically.
5. Escalate to the Financial Service owner for authoritative discrepancies and
   require maker/checker plus a separately approved correction workflow.
6. Rerun all ten checks after resolution. Readiness remains blocked until the
   report is clean and audit continuity is proven.

Block activation readiness, alert on the bounded violation type, create incident evidence, and compare read-only snapshots. Never mutate either ledger as an automated repair. Resolution requires authoritative confirmation and review.
