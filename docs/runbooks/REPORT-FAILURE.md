# Report failure

Confirm job owner/tenant and safe failure class; never expose object paths or service errors. Verify reconciliation and source authority, retry idempotently, and dead-letter persistent failures for operators. Do not issue an unreconciled statement or silently replace an issued version.
