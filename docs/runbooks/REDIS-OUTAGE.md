# Redis outage

Do not default quotas, sessions, or security policy to unlimited. Move to the policy-defined restricted mode, restore Redis, invalidate unsafe cache assumptions, and verify authoritative state remains in PostgreSQL.
