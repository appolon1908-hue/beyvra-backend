# NATS outage

Retain committed events in the transactional outbox, stop uncontrolled retries, restore broker connectivity, publish idempotently, inspect consumer lag/redelivery, and reconcile for gaps.
