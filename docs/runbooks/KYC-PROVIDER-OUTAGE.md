# KYC provider outage

Confirm provider health without sending PII; alert on availability failures. Keep new sessions fail-closed as `PROVIDER_NOT_AVAILABLE`. Do not change existing approved state unless expiry/review policy requires it. Queue no unencrypted payloads. Escalate to compliance/security owners and record recovery evidence.
