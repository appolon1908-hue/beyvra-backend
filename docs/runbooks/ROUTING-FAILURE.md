# Routing Failure

Confirm global halt, market freshness, risk snapshot, capabilities, venue increments, governance and health. Preserve the evidence hash. Never select an arbitrary provider when no route is eligible.
