# Configuration drift

Compare safe normalized hashes, identify owner and source, reject secret-value disclosure, restore declared non-secret configuration through controlled deployment, and issue a new release manifest if candidate state changed.
