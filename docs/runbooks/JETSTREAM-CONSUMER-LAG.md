# JetStream consumer lag

Inspect pending, ack-pending, redelivery, and slow-consumer evidence. Apply backpressure, recover or replace the durable consumer, and verify idempotent replay. Never discard committed events.
