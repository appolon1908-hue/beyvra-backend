# Broker Halt

An authorized operator supplies an incident reason to the halt endpoint. Verify new previews exclude the provider. Resume only after health and reconciliation pass; resume cannot authorize live mode.
