# Subaccount isolation failure

Disable affected institutional access, preserve logs without owner PII, run
read-only reconciliation, and notify security. Do not merge or delete accounts.
Resume only after tenant, entitlement, IDOR, and audit tests pass.
