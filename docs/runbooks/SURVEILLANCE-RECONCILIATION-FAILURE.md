# Surveillance Reconciliation Failure

Run the read-only command and classify the exact invariant. Freeze affected simulated mutation paths when enforcement or evidence integrity is uncertain. Do not auto-repair orders, events, cases, outbox, inbox or audit. Preserve a database snapshot and escalate by violation type. Recovery requires an authorized correction workflow, rerun PASS and audit continuity.
