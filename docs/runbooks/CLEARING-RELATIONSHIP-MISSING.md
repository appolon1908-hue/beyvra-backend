# Clearing relationship missing

Deny the candidate route with `FEATURE_DISABLED`, confirm no outbound request,
and ask clearing operations to add verified reference metadata. Never fabricate
a clearer relationship or infer it from account naming.
