# API outage

Confirm `/health` and `/ready`, dependency health, release identity, error rate, and saturation. Remove unready instances, preserve evidence, recover the approved candidate, reconcile, and close only after stable readiness. Never weaken checks or enable real-value flags.
