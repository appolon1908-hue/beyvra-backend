# Account freeze

Verify tenant/account, select PARTIAL or FULL, provide canonical reason, and confirm audit/event creation. Check every sensitive mutation returns `ACCOUNT_FROZEN`. To release, open an expiring unfreeze request with review evidence; a different security manager approves and verifies enforcement. Escalate audit gaps immediately.
