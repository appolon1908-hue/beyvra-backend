# Compliance override

Record control, previous/new state, reason, maker, requested time, and expiry. An independent authorized checker must approve sensitive overrides; self-approval is invalid. Reconcile audit/outbox and expiry behavior before closing the case.
