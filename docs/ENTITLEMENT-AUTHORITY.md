# Entitlement authority

Resolution is server-side and fail-closed: global safety and compliance restrictions, account restriction, approved override, contract assignment, plan version, then default deny. Decisions are `ALLOW`, `DENY`, or `LIMITED`. Plans never override real-money or execution safety gates.
