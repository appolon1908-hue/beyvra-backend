# Institutional risk aggregation

Risk evaluates both institution and child status and returns a versioned,
simulation-only decision. A restricted parent denies an otherwise-active child.
Gross/net projection cannot bypass the stricter parent control.
