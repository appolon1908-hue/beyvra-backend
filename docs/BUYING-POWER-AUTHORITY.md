# Buying power authority

`BuyingPowerService` combines simulation equity, eligible collateral, margin used, and open-order reservations. It returns an explicit allow/deny impact and never derives real ledger equity.
