# Allocation group authority

Allocation groups use Decimal weights and effective membership. Fixed-percent
groups require weights totaling exactly one. Members must belong to the same
institution. Supported methods are pro-rata, fixed percent/quantity,
order-specified, strategy-defined, and manual review.
