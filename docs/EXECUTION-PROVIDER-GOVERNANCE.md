# Execution Provider Governance

Eligibility requires enabled state, exact environment, paper approval, healthy circuit, valid provider/venue capability and no global halt. Live mode, live capability and `LIVE_APPROVED` records are prohibited in this candidate. There is no live-enable API.
