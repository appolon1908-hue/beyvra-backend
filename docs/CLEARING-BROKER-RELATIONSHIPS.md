# Clearing broker relationships

Provider-neutral broker references and relationship types are effective-dated.
Paper approval is separate from live approval. `approved_for_live` is
non-editable and defaults false. No relationship creates or proves a real
broker or clearing account.
