# Risk RBAC

Customer routes are self-scoped and simulation-safe. Operator mutation remains disabled. Future risk viewer, analyst, and manager roles require tenant scoping and maker/checker; no self-approval.
