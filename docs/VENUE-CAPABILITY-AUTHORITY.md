# Venue Capability Authority

Venue routing validates active status, asset class, order type, TIF, session, minimum quantity, quantity increment and price increment. Provider support never implies venue support. `BEYVRA-SIM` is the only fixture venue and is not external.
