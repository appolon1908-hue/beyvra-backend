from django.apps import AppConfig


class RealWalletConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "real_wallet"
    label = "real_wallet"
    verbose_name = "Real wallet and ledger boundary"
